const initailComments = {
    comment : '',
    loading : '',

}

const CommentReducer = (state, action)=>{
    switch (action.type) {
        case 'add-comment':
            
            break;
    
        default:
            break;
    }
}